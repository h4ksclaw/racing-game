// Re-export shared types for client convenience

export { CAMERA, DRIFT, NETWORK, PHYSICS } from "@shared/constants.ts";
export type {
	CarState,
	ControlState,
	DriftState,
	LobbyState,
	NetworkMessage,
	PlayerInfo,
} from "@shared/types.ts";
