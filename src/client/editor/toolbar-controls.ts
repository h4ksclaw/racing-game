/**
 * Toolbar controls — deprecated. Logic moved to EditorToolbar Lit component.
 * This file is kept as a no-op for any imports that reference it.
 */

export function initToolbarControls(): void {
	// All toolbar logic now handled by <editor-toolbar> Lit component in editor-ui.ts
}
